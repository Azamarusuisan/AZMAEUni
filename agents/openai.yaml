interface:
  display_name: "Note Draft Pipeline"
  short_description: "URL調査・文字入り画像からnote下書き保存まで安全に実行"
  default_prompt: "Use $write-note-drafts to open my supplied URLs, create the article and verified text-bearing images, then save and reread one unpublished note draft."

policy:
  allow_implicit_invocation: true
